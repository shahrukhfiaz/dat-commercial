# Digital Storming Loadboard V2 - Server Configuration
# NEW SERVER DEPLOYMENT (Separate from old production)

# Server Configuration
NODE_ENV=production
PORT=3000
API_VERSION=v1

# NEW Database Configuration (Neon PostgreSQL)
DATABASE_URL=postgresql://neondb_owner:npg_qHPECT6yZgd7@ep-tiny-bush-a012rwq3-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require

# JWT Configuration
JWT_ACCESS_SECRET=your-super-secret-jwt-access-key-change-this-in-production-min-32-chars
JWT_REFRESH_SECRET=your-super-secret-jwt-refresh-key-change-this-in-production-min-32-chars
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# CORS Configuration - NEW SERVER IP
CORS_ORIGIN=http://67.205.189.32:3000,http://localhost:3000

# Super Admin Bootstrap
SUPER_ADMIN_EMAIL=superadmin@digitalstorming.com
SUPER_ADMIN_PASSWORD=ChangeMeSuperSecure123!

# Object Storage Configuration (DigitalOcean Spaces or AWS S3)
OBJECT_STORAGE_ENDPOINT=https://nyc3.digitaloceanspaces.com
OBJECT_STORAGE_BUCKET=ds-loadboard-sessions-v2
OBJECT_STORAGE_ACCESS_KEY=your_spaces_access_key_here
OBJECT_STORAGE_SECRET_KEY=your_spaces_secret_key_here
OBJECT_STORAGE_REGION=nyc3

# Session Bundle Encryption (optional)
SESSION_BUNDLE_ENCRYPTION_KEY=base64encoded32bytekey

# Playwright Configuration (for DAT session seeder)
DAT_MASTER_EMAIL=your-dat-email@example.com
DAT_MASTER_PASSWORD=YourDATPassword123

# Instructions:
# 1. Generate secure JWT secrets:
#    node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
# 2. Set up DigitalOcean Spaces or AWS S3 for session storage
# 3. Update DAT master credentials for session seeder
# 4. Copy this file to .env: cp NEW_SERVER_CONFIG.env .env
# 5. Run migrations: npm run db:migrate
# 6. Start server: npm run start

